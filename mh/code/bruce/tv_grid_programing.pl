

    if (time_now '4/09 19:30 - 00:02') {
        speak "rooms=all $Time_Now. VCR recording will be started in 2 minutes for Robot Wars";
    }
    if (time_now '4/09 19:30') {
        speak "VCR recording started";
        print_log "VCR recording on channel 2 for Robot Wars";
        set $VCR "2,RECORD";
#       run('min', 'IR_cmd VCR,2,RECORD');
    }
#   if (time_now '4/09 20:00 - 00:01') {
    if (time_now '4/09 20:00') {
        speak "VCR recording stopped";
        set $VCR "2,STOP";
#       run('min', 'IR_cmd VCR,STOP');
    }


    if (time_now '8/12 20:00 - 00:02') {
        speak "rooms=all $Time_Now. VCR recording will be started in 2 minutes for NFL Preseason Football";
    }
    if (time_now '8/12 20:00') {
        speak "VCR recording started";
        print_log "VCR recording on channel 8 for NFL Preseason Football";
        set $VCR "8,RECORD";
#       run('min', 'IR_cmd VCR,8,RECORD');
    }
#   if (time_now '8/12 22:00 - 00:01') {
    if (time_now '8/12 22:00') {
        speak "VCR recording stopped";
        set $VCR "8,STOP";
#       run('min', 'IR_cmd VCR,STOP');
    }


    if (time_now '8/19 19:00 - 00:02') {
        speak "rooms=all $Time_Now. VCR recording will be started in 2 minutes for Ferris Bueller";
    }
    if (time_now '8/19 19:00') {
        speak "VCR recording started";
        print_log "VCR recording on channel 36 for Ferris Bueller";
        set $VCR "36,RECORD";
#       run('min', 'IR_cmd VCR,36,RECORD');
    }
#   if (time_now '8/19 21:30 - 00:01') {
    if (time_now '8/19 21:30') {
        speak "VCR recording stopped";
        set $VCR "36,STOP";
#       run('min', 'IR_cmd VCR,STOP');
    }


    if (time_now '8/26 16:00 - 00:02') {
        speak "rooms=all $Time_Now. VCR recording will be started in 2 minutes for Battlebots";
    }
    if (time_now '8/26 16:00') {
        speak "VCR recording started";
        print_log "VCR recording on channel 47 for Battlebots";
        set $VCR "47,RECORD";
#       run('min', 'IR_cmd VCR,47,RECORD');
    }
#   if (time_now '8/26 16:30 - 00:01') {
    if (time_now '8/26 16:30') {
        speak "VCR recording stopped";
        set $VCR "47,STOP";
#       run('min', 'IR_cmd VCR,STOP');
    }


    if (time_now '8/27 10:30 - 00:02') {
        speak "rooms=all $Time_Now. VCR recording will be started in 2 minutes for Battlebots";
    }
    if (time_now '8/27 10:30') {
        speak "VCR recording started";
        print_log "VCR recording on channel 47 for Battlebots";
        set $VCR "47,RECORD";
#       run('min', 'IR_cmd VCR,47,RECORD');
    }
#   if (time_now '8/27 11:00 - 00:01') {
    if (time_now '8/27 11:00') {
        speak "VCR recording stopped";
        set $VCR "47,STOP";
#       run('min', 'IR_cmd VCR,STOP');
    }


    if (time_now '10/02 19:30 - 00:02') {
        speak "rooms=all $Time_Now. VCR recording will be started in 2 minutes for Tucker";
    }
    if (time_now '10/02 19:30') {
        speak "VCR recording started";
        print_log "VCR recording on channel 12 for Tucker";
        set $VCR "12,RECORD";
#       run('min', 'IR_cmd VCR,12,RECORD');
    }
#   if (time_now '10/02 20:00 - 00:01') {
    if (time_now '10/02 20:00') {
        speak "VCR recording stopped";
        set $VCR "12,STOP";
#       run('min', 'IR_cmd VCR,STOP');
    }


    if (time_now '10/06 20:00 - 00:02') {
        speak "rooms=all $Time_Now. VCR recording will be started in 2 minutes for Freakylinks";
    }
    if (time_now '10/06 20:00') {
        speak "VCR recording started";
        print_log "VCR recording on channel 8 for Freakylinks";
        set $VCR "8,RECORD";
#       run('min', 'IR_cmd VCR,8,RECORD');
    }
#   if (time_now '10/06 21:00 - 00:01') {
    if (time_now '10/06 21:00') {
        speak "VCR recording stopped";
        set $VCR "8,STOP";
#       run('min', 'IR_cmd VCR,STOP');
    }


    if (time_now '10/06 20:00 - 00:02') {
        speak "rooms=all $Time_Now. VCR recording will be started in 2 minutes for Freakylinks";
    }
    if (time_now '10/06 20:00') {
        speak "VCR recording started";
        print_log "VCR recording on channel 8 for Freakylinks";
        set $VCR "8,RECORD";
#       run('min', 'IR_cmd VCR,8,RECORD');
    }
#   if (time_now '10/06 21:00 - 00:01') {
    if (time_now '10/06 21:00') {
        speak "VCR recording stopped";
        set $VCR "8,STOP";
#       run('min', 'IR_cmd VCR,STOP');
    }

